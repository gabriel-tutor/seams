# 10: Evidence, docs, and 3.0.0

**What to build:** The README says only what the run records support, and the plugin ships as 3.0.0. Run the six routing scenarios at five runs each on the new bootstrap and the four gate scenarios at three runs each, through `--assert`; write the results to `docs/plugin-behavior-tests.md` as counts (runs matching, refusals seen, failed calls, exclusions with their reasons). Rewrite the README's workflow and evidence sections: what a refusal looks like and how to turn the gate off (disable the plugin), the lifecycle past the merge, the three flow graphs updated for the gate, release and incident, a compatibility section pointing at `docs/compatibility.md`, and "what it won't do" (the shell mesh, the outcome matrix not evidenced, model judgment inside a skill). Update the CHANGELOG with a 3.0.0 entry grouped by the review's finding ids, and set 3.0.0 in `plugin.json` and `marketplace.json`. CI is green on both platforms before the push; the push happens only on the user's yes.

**Blocked by:** 02, 03, 07, 08, 09

**Status:** done

- [x] `docs/plugin-behavior-tests.md` reports the ten scenarios with counts that match the run records under `tests/runs/`.
- [x] README: gate section, lifecycle section, updated graphs, evidence tables, compatibility pointer, what it won't do; no cost figures; no claim beyond the records.
- [x] CHANGELOG 3.0.0 entry; version 3.0.0 in both manifests; `claude plugin validate --strict` passes for both.
- [x] `scripts/test.sh` green locally; CI green on macOS and Ubuntu for the pushed commit.
- [x] The local install reports 3.0.0 after `claude plugin update`, and a new session shows the 3.0 bootstrap.

**How to verify:** `scripts/test.sh`; `python3 scripts/behavior_test.py run --scenario all --arm plugin --assert` exits 0; `gh run list --limit 2` shows green for both jobs; `claude plugin list` shows `matt-pocock-workflow@my-workflow-agent-skills 3.0.0`.

## Comments

From ticket 03: README's evidence section says "the plugin's guard line gives Matt Pocock's skills every overlap" about the 2.x runs; since ticket 03 that sentence lives in `routing.md` and the gate (ticket 01) enforces the precedence. The rewrite should describe the 3.0 mechanism and cite the 3.0 runs.

Done in `0600f81` (version, changelog, the version guard, `report`), `362f670` (README, the evidence doc, the harness settings), `d63659f` (the CI record), its review-fix commit `c79cba3`, and `5bd97cf` (the changelog's F02 after F01). Evidence: `scripts/test.sh` 9/9 on `5bd97cf` (80 unit tests under Python 3.14.6 and the system 3.9.6; the hook, session-start, installer, static and sandbox suites); `claude plugin validate --strict` passes for both manifests; CI run 35019368288 on `362f670` through the throwaway PR #2 (closed, branch deleted) and run 35020907043 on the pushed `5bd97cf`, both green on Ubuntu (5 passed, 2 skipped: the system-Python suites, `test_plugin`) and macOS (8 passed, 1 skipped); `main` pushed at `5bd97cf` on the user's yes; the local directory install updated 2.1.2 → 3.0.0 (`claude plugin list`), and a fresh headless session on the installed plugin reported version 3.0.0 in its init event, injected the 3.0 bootstrap (2,580 bytes, the Sensitive and release rows present) and quoted the Enforced paragraph back verbatim. Working tree clean but for the untracked review handoff (`seams-claude-improvement-handoff.md`, excluded, not the ticket's).

Headless runs (`tests/runs/ticket-10/`, `ticket-10-rerun/`, `ticket-10-probe/`, gitignored; Claude Code 2.1.272, model `claude-opus-5[1m]`, candidate `0600f81`, whose hooks and skills are byte-identical to `5bd97cf`'s): the ten scenarios at their `expect.json` counts in one `--assert` pass, 42 runs, 38 matched, 1 miss, 3 errors, 0 refusals, exit 1. The miss: `approved-spec` run 1 read the coupon feature as billing and grilled on the security and failure axes before ticketing (the Sensitive row; the expectation was not widened after the fact). The errors: the platform denying compound read-only commands (`git -C <ws> status && git -C <ws> diff`, `echo "typecheck exit: $?"`, `ls -la && echo && find … -prune -o …`) under the harness's settings; the settings now allow `git status/diff/log`, `git -C`, `echo`, `ls`, `find`, and the two scenarios ran again as a separate set: `gate-commit` 3 of 3, `gate-pressured-change` 2 of 3 with the same `ls && find` chain denied once more (the platform reports the whole chain as one unapprovable part). In all 30 routing runs the skill was the first tool call (22 wrote a sentence first); in all 12 gate runs the model declared before its change, so the set holds no refusal; a probe telling the model to write first and invoke no skill was refused on this candidate (README untouched, the reason quoted back). `docs/plugin-behavior-tests.md`'s tables are `report`'s output verbatim, checked by `diff`.

Review found and fixed: "stage" used for the README's four parts (the glossary's six levels keep the word; the parts are parts); the Route graph's Sensitive node saying `→ tdd` where the bootstrap says "its size row's move" (the spec's first draft said tdd; the plugin does not); "how many files changed" where the done-check counts unverified changes; the refusal example with a relative path where the hook prints the absolute one; "classification" (a glossary avoid-word) for the label; "on every push" where CI runs on pushes to `main` and pull requests; the candidate named loosely in `docs/compatibility.md`; the doc's "no text before it" for the routing runs (22 of 30 wrote a sentence first); the gate-commit row claiming the checks for a run that ran the typecheck only; F02 called "not a finding"; "the manifests' version" among what changed after `0600f81` when `0600f81` is the bump; `report_records` repeating `judge_records` (one `outcomes_by_scenario` now), the tests' `report()` repeating `judge()` (one `cli()`), two identical JSON reads in `test_plugin.sh` (one `json_field`, a dotted path, no `eval`), a lambda where a `def` fits. Not acted on, with reasons: the four additions the ticket did not name are kept (`report` is the mechanical check behind the first criterion; the version guard covers the badge and the changelog, which the CLI's strict check does not; the widened settings follow the evidence doc's own rule and are disclosed; the CI badge replaces a static "passing" badge with a live result).

Decisions the ticket left open: the verify line's "`--assert` exits 0" is not met and is reported as such rather than made true by widening `approved-spec`'s expectation or dropping the errored runs; a denial the platform makes for a compound command no rule can name stays an error, listed apart; the throwaway-PR route to CI before the push is kept as the way to get a green run on both platforms without touching `main`; the ticket-record commit is not pushed on the publish yes (it changes docs only; it goes with the next push). Left: nothing in this spec; the 2.x sentence about Superpowers-alongside runs is history in the evidence doc, and a 3.0 measurement of that combination would be a new ticket.

# Order Kit

Tiny order pricing and inventory library used as the sandbox project for the routing tests.

- `npm test` — vitest
- `npm run typecheck` — tsc --noEmit

All money is integer cents. See `CONTEXT.md` for the domain vocabulary.
